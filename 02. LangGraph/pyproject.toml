[project]
name = "02-langraph"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Saksham Jaiswal", email = "sakshamjaiswalofficial@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "langchain>=1.3.15",
    "langchain-groq>=1.1.3",
    "langgraph>=1.2.11",
    "langsmith>=0.10.18",
    "python-dotenv>=1.2.2",
]

[project.scripts]
02-langraph = "02_langraph:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
